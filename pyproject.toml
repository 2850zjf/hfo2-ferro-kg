[project]
name = "hfo2-ferro-kg"
version = "0.1.0"
description = "Local HfO2-based ferroelectric material knowledge graph workbench."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
