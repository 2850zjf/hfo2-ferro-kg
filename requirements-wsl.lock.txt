# WSL environment lock file for HfO2-FerroKG
#
# Generated 2026-09-12 from the venv actually used to run the test suite:
#   /home/hautz/.venvs/hfo2-ferrokg   (WSL Ubuntu-24.04, Python 3.12.3, pip 26.2.1)
#   verified by: 197 passed, 0 skipped  (pytest 9.0.3)
#
# Recreate with:
#   python3.12 -m venv ~/.venvs/hfo2-ferrokg
#   ~/.venvs/hfo2-ferrokg/bin/python -m pip install -r requirements-wsl.lock.txt
#
# ---------------------------------------------------------------------------
# WHY THIS FILE EXISTS
# ---------------------------------------------------------------------------
# requirements.txt is not a faithful description of any environment that has
# ever worked. Both copies in this repo are wrong, in different directions:
#
#   * the worktree copy declares 12 packages, all ">=" with no upper bound, and
#     OMITS numpy (imported by 7 files), pillow and requests (imported by 1
#     file each). It declares networkx, which nothing imports.
#   * the main-repo copy declares plotly and dashscope, neither of which was
#     present in the macOS venv.
#
# The last known-good environment was the macOS venv at
#   <main repo>/.venv    (Python 3.12.13, 84 distributions)
# Installing from requirements.txt today would resolve to newer, untested
# versions of a stack that includes pandas 3.0 and numpy 2.4 - both
# major-version breaking releases. So the 16 direct dependencies below were
# pinned to the versions measured in that macOS venv, and this file records the
# full resolved set including transitives.
#
# Direct pins that match the macOS venv exactly:
#   PyMuPDF 1.27.2.3   pdfplumber 0.11.9   pandas 3.0.3      matplotlib 3.11.0
#   pydantic 2.13.4    python-dotenv 1.2.2 PyYAML 6.0.3      streamlit 1.58.0
#   networkx 3.6.1     scikit-learn 1.9.0  pytest 9.0.3      openai 2.40.0
#   numpy 2.4.6        pillow 12.2.0       requests 2.34.2
#
# Transitive dependencies were NOT pinned during install and therefore drifted
# from the macOS venv, e.g. scipy 1.18.1 (mac 1.17.1), pyarrow 25.0.1 (24.0.0),
# cryptography 50.0.1 (48.0.0), gitpython 3.1.62 (3.1.50). This file freezes
# the versions that were actually tested, so a rebuild is reproducible even
# though it is not byte-identical to the macOS environment.
#
# ---------------------------------------------------------------------------
# DELIBERATE DEVIATION FROM THE macOS VENV
# ---------------------------------------------------------------------------
# spglib==2.7.0 is installed here but was ABSENT from the macOS venv, even
# though:
#   * reports/phase_strain_computation_preflight_20260824/REMOTE_EXECUTION_GATE.md:148
#     claims the nine generated POSCAR files "passed a hash-bound spglib 2.7.0
#     precheck at symprec=1e-3";
#   * docs/phase_competition_research_protocol.md section 9 requires independent
#     space-group verification of the frozen m/o/t starting structures.
# backend/services/vasp_raw_audit.py guards the import with
# try/except ModuleNotFoundError, so its absence degraded silently rather than
# failing. Installing it makes tests/test_vasp_raw_audit.py and
# tests/test_phase_strain_job_builder.py exercise the real symmetry path.
#
# ---------------------------------------------------------------------------
# DELIBERATELY NOT INSTALLED
# ---------------------------------------------------------------------------
# FerroX phase-field stack (ase, jax, jaxlib, cmake, and their deps) - a
# separate sub-project; see scripts/install_ferrox_wsl.ps1. Without it,
# app/pages/14_FerroX_相场模拟.py is non-functional.
#
# pymatgen, mp-api - only used by the standalone script
# computations/mp_hfo2_phase_smoke_test/fetch_mp_structures.py, which was
# likewise absent from the macOS venv.
#
# ---------------------------------------------------------------------------
# RESOLVED SET (pip freeze, 79 distributions)
# ---------------------------------------------------------------------------
altair==6.2.2
annotated-types==0.8.0
anyio==4.15.1
attrs==26.1.0
blinker==1.9.0
cachetools==7.1.8
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.5.0
cloudpickle==3.1.2
contourpy==1.3.3
cryptography==50.0.1
cycler==0.12.1
distro==1.9.0
fonttools==4.65.0
gitdb==4.0.12
GitPython==3.1.62
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.19
iniconfig==2.3.0
itsdangerous==2.2.0
Jinja2==3.1.6
jiter==0.16.0
joblib==1.6.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
kiwisolver==1.5.1
MarkupSafe==3.0.3
matplotlib==3.11.0
narwhals==2.26.0
networkx==3.6.1
numpy==2.4.6
openai==2.40.0
packaging==26.3
pandas==3.0.3
pdfminer.six==20251230
pdfplumber==0.11.9
pillow==12.2.0
pluggy==1.6.0
protobuf==7.36.1
pyarrow==25.0.1
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
pydeck==0.9.3
Pygments==2.21.0
PyMuPDF==1.27.2.3
pyparsing==3.3.2
pypdfium2==5.13.0
pytest==9.0.3
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
python-multipart==0.0.32
PyYAML==6.0.3
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
scikit-learn==1.9.0
scipy==1.18.1
six==1.17.0
smmap==5.0.3
sniffio==1.3.1
spglib==2.7.0
starlette==1.6.0
streamlit==1.58.0
tenacity==9.1.4
threadpoolctl==3.6.0
toml==0.10.2
tqdm==4.70.1
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.4
watchdog==6.0.0
websockets==17.1
